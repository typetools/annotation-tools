#!/bin/sh

# Concatenates multiple descriptions of annotations into a single one.

SCENE_LIB=$(dirname $0)
WORKSPACE=$SCENE_LIB/..
ASMX=$WORKSPACE/asmx

export CLASSPATH=$SCENE_LIB/bin:$ASMX/bin:$WORKSPACE/annotation-file-utilities/lib/utilMDE.jar:$CLASSPATH
java annotations.tools.Anncat "$@"
